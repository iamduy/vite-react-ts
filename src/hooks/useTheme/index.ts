// import { useZustandTheme } from '@/states';

// export const useTheme = () => {
//   const theme = useZustandTheme((state) => state.theme);
//   const setTheme = useZustandTheme((state) => state.setTheme);

//   return { theme, setTheme };
// };
